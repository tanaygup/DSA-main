/*HEAPS
complete binary tree(every level is completely filled except last level, nodes always added from left)

represented by stack of k size

IDENTIFICATION ->
1. K   2. smallest(MAX HEAP)/ largest(MIN HEAP) to find out

max heap -> max element at top
min heap -> min element at top

TC -> nlogk
*/
#include<iostream>
#include<queue>
using namespace std;
int main() {
    priority_queue<int> maxHeap;
    priority_queue<int,vector<int>, greater<int>> minHeap;

    

return 0;
}
